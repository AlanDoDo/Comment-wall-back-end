const config = {
    // 启动端口号
    port: 3004,
    database: {
        HOST: 'localhost',
        USER: 'root',
        PASSWORD: 'root',
        WALL: 'WALLTS',
    }
}

module.exports = config;